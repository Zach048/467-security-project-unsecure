<nav class="navbar navbar-expand-lg navbar-light bg-light">
   <div class="collapse navbar-collapse" id="navbarSupportedContent">
     <ul class="navbar-nav mr-auto">
       <li class="nav-item">
         <a class="nav-link" href="/dashboard/{{id}}">Dashboard</a>
       </li>
       <li class="nav-item active">
         <a class="nav-link" href="/transactions/{{id}}">Transactions</a>
       </li>
       <li class="nav-item">
         <a class="nav-link" href="/credit/{{id}}">Pay Credit Card</a>
       </li>
       <li class="nav-item">
         <a class="nav-link" href="/personal/{{id}}">Personal Information</a>
       </li>
     </ul>
     <ul class="navbar-nav navbar-right">
       <li class="nav-item">
         <a class="nav-link" href="/">Log Out</a>
       </li>
     </ul>
  </div>
</nav>

<div class="container">
  <table class="table table-striped table-responsive-md btn-table">
    <thead>
      <tr>
        <th scope="col">Transaction ID</th>
        <th scope="col">Vendor Name</th>
        <th scope="col">Amount Paid</th>
      </tr>
    </thead>
    <tbody>
      {{#each transactions}}
      <tr>
        <td scope="row">{{ this.id }}</td>
        <td>{{ this.vendor_name }}</td>
        <td>{{ this.amount_paid }}</td>
      </tr>
      {{/each}}
    </tbody>
  </table>
</div>